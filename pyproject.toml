[project]
name = "ccx-data-pipeline"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "insights-core>=3.6.11",
    "ccx-ocp-core",
    "ccx-messaging==4.3.6",
    "ccx-rules-ocp",
    "boto3>=1.42.66,<1.43.0" # limited due to incompatibility between s3fs and newer versions of boto
]
